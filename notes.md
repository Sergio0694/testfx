# Perf Improver — Repo Notes (testfx)

## Perf-relevant techniques observed in this codebase
- Assertion hot paths use `TelemetryCollector.TrackAssertionCall` (opt-out via env vars, `[MethodImpl(AggressiveInlining)]`, `Lazy<bool>` for the opt-out check) — good pattern to mirror for new hot-path instrumentation.
- `MSTestTestNodeConverter` caches `BaseTestNodeData` per `UnitTestElement` via `ConditionalWeakTable` to avoid repeated recomputation across discovery/execution phases.
- `TestDataSourceUtilities` uses `[ThreadStatic]` `StringBuilder` reuse (capped at `MaxCachedBuilderCapacity = 360`) plus a `ConditionalWeakTable<MethodInfo, MethodData>` cache for per-data-row display name computation — a solid pattern for hot reflection-adjacent paths.
- `Assert.AreEquivalent` (the newer structural-equivalence comparer, distinct from `CollectionAssert.AreEquivalent`) already caches reflection metadata (`MemberCache`, `IEquatableEqualsCache`, `EnumerableElementTypeCache`, `DictionaryValueTypeCache`, `IsPrimitiveLikeCache`) via `ConcurrentDictionary`-based caches keyed by `Type`. Well optimized already.

## Completed work
### 2026-09-18 (run 35350743060)
- Found & fixed: `CollectionAssert.Helpers.GetElementCounts<T>` (used by `CollectionAssert.AreEquivalent`, `AreNotEquivalent`, `IsSubsetOf`) built its internal `Dictionary<T,int>` with default (0) capacity regardless of known input size, causing avoidable resizes/rehashes. Fixed by sizing the dictionary from `ICollection<T>.Count`/`ICollection.Count` when available.
- Measured via `CollectionAssertEquivalenceBenchmarks` (in-process, ShortRun, net10.0 Release):
  - Small (5 elements): 916ns/856B -> 309ns/632B
  - Large (1000 elements): 69.0µs/146,424B -> 32.7µs/44,472B (~2x faster, ~70% less allocation)
- Verified: `TestFramework.UnitTests` full suite (1560 tests) passes; all 54 `CollectionAssertTests` pass; `dotnet format --verify-no-changes` shows no new warnings.
- PR created via safe-outputs from branch `perf-assist/collectionassert-equivalence-dict-capacity` (title: "[perf-improver] Pre-size dictionaries in CollectionAssert element-count comparisons"). Awaiting PR number from safe-outputs processing — check next run's `session_store_sql`/`github` list_pull_requests for actual number since local `gh` is unauthenticated.

## Optimization backlog (not yet attempted, prioritized)
1. **[Feasible, low-risk]** `CollectionAssert.Helpers.CompareIEnumerable` (used by `CollectionAssert.AreEqual`) uses a `Stack<Tuple<IEnumerator,IEnumerator,int>>` per nested-enumerable comparison — `Tuple` (reference type) allocates per push; could switch to a value-tuple-based stack or a lightweight struct to cut allocations for nested-collection equality checks. Needs its own benchmark (none exists yet) before implementing — good Task 6 candidate (add a `CollectionAssertEqualityBenchmarks` class first).
2. **[Needs investigation]** `IsSubsetOfHelper`/`FindMismatchedElement` still count via boxing dictionaries when `T` is a value type used through `ICollection` (`object`-typed path in `IsSubsetOfHelper` specifically, since it operates on non-generic `ICollection`). Boxing is inherent to the non-generic `ICollection`-based overloads of `CollectionAssert.IsSubsetOf`/`AreEquivalent` and can't be removed without an API/behavior change — not attempted, flagged as a structural limitation rather than a fixable perf bug.
3. **[Build/CI perf]** Not yet investigated: build parallelism, incremental build, or CI duration for this repo. Next run should check `.github/workflows/*.yml` for build/test timing and whether `-m`/graph build flags are already used.
4. Have not yet explored MTP discovery/execution hot paths beyond `MSTestTestNodeConverter` (already fast). Consider profiling actual end-to-end discovery of a large test project next.

### 2026-09-19 (run 35445953270) — attempted, reverted (no PR)
- Attempted backlog item #1: replaced `Stack<Tuple<IEnumerator,IEnumerator,int>>` with `Stack<(IEnumerator,IEnumerator,int)>` (value tuple) in `CollectionAssert.Helpers.CompareIEnumerable` (used by `CollectionAssert.AreEqual`/`AreNotEqual`).
- Added new `CollectionAssertEqualityBenchmarks` (shallow + deep nested-collection cases) to `test/Performance/MSTest.Performance.Benchmarks`.
- **Isolated micro-benchmark confirmed the Tuple→ValueTuple swap itself is a real win**: 1M push/pop cycles on a bare `Stack<Tuple<int,int,int>>` allocate ~32 bytes/op vs ~0 bytes/op for `Stack<(int,int,int)>` (verified with `GC.GetAllocatedBytesForCurrentThread`, .NET 9, Release).
- **However, at the `CollectionAssert.AreEqual` call level (5000 nested arrays), the fix showed no measurable end-to-end improvement**: baseline 847.7 µs/1.62 MB (short) and 859.0 µs/1.62 MB (medium) vs. fixed 1.52 ms/1.74 MB (short) and 1.357 ms/1.74 MB (medium) — i.e. *higher* time and allocation with the fix, dominated by run-to-run noise/GC variance (MultimodalDistribution warning from BenchmarkDotNet) rather than a real regression. The per-push Tuple allocation (~24 bytes each, 3 fields + header) is dwarfed by the far larger per-element `IComparer.Compare`/boxing costs already present in `CompareIEnumerable`'s hot loop, so the theoretical stack-frame allocation savings don't surface as a measurable win at realistic collection sizes.
- **Learning**: Don't trust isolated micro-benchmark wins as proxies for real callsite impact — always benchmark at the actual public-API call site with a realistic workload before creating a PR. Reverted the change (`git checkout --`) and removed the new benchmark file; no PR created for this attempt.
- Backlog item #1 is now considered **not worth pursuing** as a standalone change — deprioritized/removed from backlog.

### 2026-09-20 (run 35513679411)
- Backlog item #3 (CI/build parallelism) resolved as **already optimal, no action needed**: Arcade's shared `eng/common/tools.sh` `MSBuild` function already passes `/m` (multi-proc, all cores) on every invocation of `build.sh`/`build.cmd` — this was previously missed because the flag lives in the shared Arcade helper, not in a repo-level workflow/props file. Closing this backlog item permanently; do not re-investigate build parallelism unless Arcade's shared tooling changes.
- Found & fixed a second instance of the same class of bug as the PR #6 fix: `CollectionAssert.AllItemsAreUnique` built its internal `HashSet<object>` with default (0) capacity regardless of the known `ICollection.Count`, causing avoidable resizes/rehashes. Fixed by pre-sizing via `new HashSet<object>(collection.Count)` guarded by `#if NETCOREAPP3_1_OR_GREATER` (HashSet capacity ctor unavailable on netstandard2.0), falling back to `[]` on netstandard2.0.
- Added `CollectionAssertAllItemsAreUniqueBenchmarks` (small=5 elements, large=1000 elements) to `MSTest.Performance.Benchmarks`.
- Measured (in-process `-i`, ShortRun, net10.0 Release):
  - Small: 539.4 ns/520 B -> 185.4 ns/408 B
  - Large: 27.5 µs/97,184 B -> 20.2 µs/46,208 B (~27% faster, ~52% less allocation)
- Verified: `TestFramework.UnitTests` full suite (1560 tests) passes; all 54 `CollectionAssertTests` pass; both `net9.0` and `netstandard2.0` builds succeed cleanly; `dotnet format --verify-no-changes` shows no new warnings.
- PR created via safe-outputs from branch `perf-assist/collectionassert-allitemsareunique-hashset-capacity` (title: "[perf-improver] Pre-size HashSet in CollectionAssert.AllItemsAreUnique"). PR number not yet known — check next run.
- Ran a broader source scan (Assert.cs/StringAssert.cs failure-path message formatting, MTP message bus/host LINQ usage, adapter reflection/attribute caching) looking for other new candidates: nothing else stood out as both measurable and worth a standalone PR this run — failure-path formatting is already lazy (only runs on assert failure), attribute caching is already `ConcurrentDictionary`-based, and MTP message-bus LINQ usages found are on low-frequency (init/shutdown) paths, not per-test-execution hot paths.
- **New backlog idea for a future run**: scan other `CollectionAssert`/`Assert` methods for the same "collection with known Count building a Dictionary/HashSet/List with default capacity" pattern systematically (this run found it ad-hoc via source review) — e.g. check `CollectionAssert.Equality.cs`, `Assert.AreAllDistinct` variants (`Assert.AreAllDistinct.cs` already uses `HashSet<T>(comparer)` without capacity too — same class of bug, not yet fixed, is IEnumerable-typed there so Count isn't always cheaply available; would need an `ICollection`/`ICollection<T>` fast-path check before presizing). Worth a follow-up PR.

## Backlog cursor
- Item #1 (CompareIEnumerable Tuple allocation): attempted 2026-09-19, no measurable benefit at realistic scale — closed out, do not re-attempt without a different angle.
- Item #3 (CI/build parallelism): resolved 2026-09-20 — already handled by Arcade's `/m` flag in `eng/common/tools.sh`. Closed, no action needed.
- Backlog item #2 (boxing in non-generic `ICollection` overloads) remains a structural limitation, not actionable without an API change.
- **New item #5** (queued for next run): `Assert.AreAllDistinct`'s `HashSet<T>(comparer)` (in `Assert.AreAllDistinct.cs`) doesn't pre-size either, but the parameter is `IEnumerable<T>` (not `ICollection`), so a capacity hint requires an `is ICollection<T> coll ? coll.Count : 0`-style fast path (with 0 meaning "unknown, use default ctor") rather than a direct `.Count` read. Needs its own benchmark before implementing.
- No new user-facing performance issues found this run (no open issues exist in the repo at all as of 2026-09-20 — confirmed via `list_issues`).

### 2026-09-21 (run 35606414945)
- Task 4 (PR maintenance): checked open perf-improver PRs #6 and #11 — both still open/draft, no comments from anyone, no easily-accessible CI conclusion via available MCP tools (workflow-run lookup by head_sha returned unrelated results); nothing actionable this run.
- Task 3: Found & fixed a third instance of the same "known-Count collection building a Dictionary/HashSet/List with default capacity" bug class (see 2026-09-20 backlog item #5): `Assert.AreAllDistinct`'s `AreAllDistinctImpl<T>` builds `seen = new HashSet<T>(comparer)` with default capacity even though `snapshot` is already a `List<T>` with a known `Count`. Fixed by pre-sizing via `new HashSet<T>(snapshot.Count, comparer)` guarded by `#if NETCOREAPP3_1_OR_GREATER` (that overload unavailable on netstandard2.0), falling back to the default ctor there — same pattern as prior fixes.
- Added `AssertAreAllDistinctBenchmarks` (small=5 elements, large=1000 elements) to `MSTest.Performance.Benchmarks`.
- Measured (in-process `-i`, ShortRun, net10.0 Release):
  - Small: 121.8 ns/416 B -> 84.95 ns/312 B
  - Large: 12,254 ns/62,720 B -> 7,528 ns/21,824 B (~39% faster, ~65% less allocation)
- Verified: `TestFramework.UnitTests` full suite (1560 tests) passes; all 29 `AreAllDistinct`-related tests pass; both `net9.0` and `netstandard2.0` builds succeed cleanly; `dotnet format --verify-no-changes` exits 0 (only pre-existing unrelated warnings shown).
- PR created via safe-outputs from branch `perf-assist/assert-areallodistinct-hashset-capacity` (title: "[perf-improver] Pre-size HashSet in Assert.AreAllDistinct"). PR number not yet known — check next run.
- Backlog item #5 is now **resolved** (implemented). Systematic scan for further instances of this pattern (queued in 2026-09-20 notes) not yet done this run — worth a future Task 2 pass across remaining `Assert`/`CollectionAssert` methods that build collections from `IEnumerable<T>` without an `ICollection<T>` fast path.

## Round-robin task tracking
- 2026-09-18: Ran Task 1 (validated build.sh/build.sh -c Release, discovered dotnet exec pattern for unit tests, discovered benchmark project + `-i` in-process workaround), Task 2 (surveyed benchmark coverage + hot paths), Task 3 (implemented + measured + PR'd the CollectionAssert fix), Task 7 (this note + monthly issue).
- 2026-09-19: Ran Task 1 (re-validated `./build.sh -c Release` full build succeeds, ~6 min), Task 3 (attempted CompareIEnumerable Tuple optimization, measured, reverted — no PR, see notes above), Task 4 (checked PR #6 — no CI failures/comments, nothing to do; PRs #7/#8 belong to other agents, not perf-improver), Task 5 (confirmed no performance-labeled/matching issues exist), Task 7 (this note + monthly issue). Task 2/Task 6 not deeply revisited this run (time spent on Task 3 investigation/revert).
- 2026-09-20: Ran Task 1 (skipped re-validation, commands still current from prior runs), Task 2 (delegated a broad explore-agent scan of Assert/StringAssert/adapter/MTP hot paths for new candidates — found the AllItemsAreUnique HashSet capacity bug as best new candidate), Task 3 (implemented + benchmarked + PR'd the AllItemsAreUnique fix; resolved backlog item #3 CI-parallelism as a documentation-only finding, no code change needed), Task 4 (checked PR #6 — still open/draft, no CI status posted yet (`pending`), no comments, nothing actionable), Task 7 (this note + monthly issue update). Task 5/Task 6 not run this cycle (no performance-labeled issues exist to comment on; Task 6 measurement-infra work deferred to next run for round-robin fairness).
- 2026-09-21: Ran Task 4 (checked PRs #6 and #11 — both open/draft, no comments, no actionable CI signal available via MCP tools), Task 3 (implemented + benchmarked + PR'd the Assert.AreAllDistinct HashSet capacity fix, resolved queued backlog item #5), Task 7 (this note + monthly issue update). Task 1/2/5/6 not run this cycle — next run should prioritize Task 2 (systematic scan for remaining instances of the capacity-presizing pattern) and Task 6 (measurement infra) since Tasks 3/4/7 have now run 3 cycles in a row.

### 2026-09-22 (run 35734242731)
- Task 4 (PR maintenance): checked PRs #6, #11, #15 (all mine) — all still open/draft, no comments from anyone, CI status still `pending`/no accessible conclusion via MCP tools (same as prior runs). Nothing actionable.
- Task 2/3: Systematic scan (per 2026-09-21 queued follow-up) of `new HashSet<`/`new Dictionary<`/`new List<` call sites across `src/TestFramework` for the same "known-Count collection built with default capacity" pattern. Found and fixed a new instance: `CombinatorialRandomDataAttribute.GenerateValues()` (in `Attributes/DataSource/CombinatorialRandomDataAttribute.cs`) built its `HashSet<long>` (tracks already-selected random offsets to reject duplicates) with default capacity, even though the loop adds at most `Count` elements — `Count` is known upfront. Fixed via `new HashSet<long>(Count)` guarded by `#if NETCOREAPP3_1_OR_GREATER`, falling back to the default ctor on netstandard2.0 (same guard pattern as PR #6/#11/#15).
- Added `CombinatorialRandomDataBenchmarks` (SmallCount=5, LargeCount=1000) to `MSTest.Performance.Benchmarks`.
- Measured (in-process `-i`, ShortRun, net10.0 Release):
  - Small (Count=5): 736.0 ns/1.04 KB -> 590.3 ns/952 B
  - Large (Count=1000): 35,249.6 ns/103.21 KB -> 27,887.8 ns/54,712 B (~21% faster, ~47% less allocation)
- Verified: `TestFramework.UnitTests` full suite (1560 tests) passes; all 16 Combinatorial-related tests pass; both `net9.0` and `netstandard2.0` builds of `TestFramework` succeed cleanly.
- **Note on `dotnet format --verify-no-changes`**: it reported 43 `CHARSET: Fix file encoding` errors across unrelated pre-existing files in `TestFramework.csproj` — confirmed via `git stash` that these errors exist identically on unmodified `main` (not caused by this change). Our two changed/added files were both verified to have correct UTF-8 BOM via `file`/`xxd`, and neither appeared in the CHARSET error list. This appears to be a sandbox/tool environment quirk (possibly a `dotnet format` version or `.editorconfig` interaction) rather than a real repo issue — worth flagging if it recurs, but not blocking for this PR since our files are unaffected.
- Rest of the "systematic scan" from the 2026-09-20/21 backlog notes is now **complete** for `src/TestFramework` — no further instances of the pattern found there (checked `Assert.That.cs`, `Assert.That.ExpressionDetails.cs`, `Assert.That.StringHelpers.cs`, `CollectionAssert.Helpers.cs` — all either already fixed by prior PRs or use collections without a cheap-to-obtain upfront size, e.g. `Dictionary<Expression, object?>` built incrementally from unbounded expression tree traversal). A future run could extend this scan to `src/Adapter` and `src/Platform/Microsoft.Testing.Platform` (127 raw candidate `new HashSet</Dictionary</List<` call sites found there, unfiltered — would need per-site review to find genuine known-Count-but-unsized instances, likely lower density given these are less hot-path-dense areas than Assert/CollectionAssert).
- PR created via safe-outputs from branch `perf-assist/combinatorial-random-hashset-capacity` (title: "[perf-improver] Pre-size HashSet in CombinatorialRandomDataAttribute"). PR number not yet known — check next run.

## Round-robin task tracking (continued)
- 2026-09-22: Ran Task 2 (systematic pattern scan across `src/TestFramework`, completed — see above), Task 3 (implemented + benchmarked + PR'd the CombinatorialRandomDataAttribute fix), Task 4 (checked PRs #6/#11/#15 — no actionable CI/comments), Task 7 (this note + monthly issue update). Task 1/5/6 not run this cycle — next run should prioritize Task 6 (measurement infra) and Task 5 (issue comments) since those haven't run in several cycles, and consider extending the pattern scan to src/Adapter and src/Platform per the note above.

### 2026-09-23 (run 35867868000)
- Task 4 (PR maintenance): checked PRs #6, #11, #15, #18 (all mine) — all still open/draft, no comments from anyone, mergeable_state "unstable" (pre-existing CI check gating, not something I can resolve without more context — `pull_request_read` doesn't expose per-check failure details and `actions_list`/`list_workflow_runs` output is too large/unfiltered to correlate cheaply by head_sha via the available MCP tools). Nothing actionable this run — consistent with prior runs' findings.
- Task 2/3: Continued the systematic scan (queued in 2026-09-22 notes) by delegating a scoped explore-agent pass over `src/Adapter/MSTest.TestAdapter` and `src/Adapter/MSTestAdapter.PlatformServices` for the same "known-Count collection built with default capacity" bug class. Found and fixed a new instance: `TypeEnumerator.GetTests()` (per-test-class discovery hot path in `Discovery/TypeEnumerator.cs`) built `foundTests` (`HashSet<string>`) and `tests` (`List<UnitTestElement>`) sized only from `descriptorMethods.Length`, ignoring the reflected-methods count from `PlatformServiceProvider.Instance.ReflectionOperations.GetRuntimeMethods(_type)` that the subsequent loop also adds to both collections. Fixed by hoisting the `GetRuntimeMethods` call once upfront (still skipped when `areAllTestMethodsSupported` is true) and using `descriptorMethods.Length + runtimeMethods.Length` to pre-size both collections, guarding the `HashSet(int capacity)` ctor with `#if NETCOREAPP3_1_OR_GREATER` (same pattern as #6/#11/#15/#18).
- Added `TypeEnumeratorBenchmarks` (50-method `[TestClass]`, in-process toolchain) to `MSTest.Performance.Benchmarks`.
- Measured (in-process `-i`, ShortRun, net10.0 Release): 21.37 µs/47.45 KB -> 18.31 µs/44.79 KB (~14% faster, ~5.6% less allocation for a 50-method class; scales with method count per class).
- Verified: `MSTestAdapter.PlatformServices.UnitTests` (net9.0) — all 34 `TypeEnumeratorTests` pass; full 1110-test suite has the same 24 pre-existing failing tests (all Windows-path-format `Deployment`/`TestRunDirectories` tests, confirmed identical before/after via diff) — a known environment limitation (Linux sandbox, Windows-path assertions), not caused by this change. Full repo build (`./build.sh -c Release`, all TFMs) succeeds cleanly.
- **New environment note**: Building/testing `MSTestAdapter.PlatformServices.UnitTests` in isolation (outside `./build.sh`) requires care — it's NOT included in `NonWindowsTests.slnf` (the filter `build.sh` uses on Linux) despite being a valid Linux-buildable project (its TFM list is `net462;net48;net8.0;net9.0`, and net462/net48 fail NU1201 against PlatformServices' Linux TFMs). Workaround: `dotnet restore <project> -p:TargetFrameworks=net9.0` then `dotnet build <project> -c Release -p:TargetFrameworks=net9.0 --no-restore` — but this can transiently break shared `artifacts/obj/*/project.assets.json` files for other projects (e.g. `MSTest.Analyzers.Package`, `TestFramework.SourceGeneration`) if restore commands with different TFM overrides interleave; fix by re-restoring the specific broken project (e.g. `dotnet restore src/Analyzers/MSTest.Analyzers.Package/MSTest.Analyzers.Package.csproj`) or rerunning a full `./build.sh -c Release` to reset all assets. Always finish with a full `./build.sh -c Release` before considering a change verified.
- PR created via safe-outputs from branch `perf-assist/typeenumerator-hashset-list-capacity` (title: "[perf-improver] Pre-size HashSet/List in TypeEnumerator.GetTests"). PR number not yet known — check next run.
- Systematic scan of `src/Adapter` per 2026-09-22's queued follow-up is now **partially complete**: `TypeEnumerator.cs` fixed (this run). Explore agent also flagged `AssemblyEnumerator.cs:86` (soft lower-bound List presize, low value) and `CompositeSourceGeneratedReflectionDataProvider.cs:179-192` (AOT source-gen merge path, cold/init-time) as lower-priority candidates not yet implemented — queue for a future run if time permits. `src/Platform/Microsoft.Testing.Platform` (127 raw candidate sites, unfiltered) still not reviewed — queue for a future Task 2 pass.

## Round-robin task tracking (continued)
- 2026-09-23: Ran Task 2 (delegated explore-agent scan of src/Adapter for the capacity-presizing pattern, found+fixed TypeEnumerator.GetTests), Task 3 (implemented + benchmarked + PR'd the TypeEnumerator fix), Task 4 (checked PRs #6/#11/#15/#18 — no actionable CI/comments), Task 7 (this note + created fresh Monthly Activity issue for September since none existed). Task 1/5/6 not run this cycle — next run should prioritize Task 6 (measurement infra) and Task 5 (issue comments, though repo currently has 0 open issues besides the new Monthly Activity one) since those haven't run in several cycles, and consider extending the src/Adapter/src/Platform pattern scan further per the queued notes above.

### 2026-09-24 (run 36006542786)
- Repo state check: 0 issues (open or closed) found via search — the prior Monthly Activity issue no longer exists (may have been deleted/unavailable); recreated `[perf-improver] Monthly Activity 2026-09` fresh with label `performance`.
- Task 4 (PR maintenance): checked PRs #6, #11, #15, #18, #20 — all still open/draft, `mergeable_state: unstable` (same pre-existing CI-gating pattern as prior runs, nothing actionable via available MCP tools).
- Task 2/3: Extended the systematic capacity-presizing scan (queued from 2026-09-23) into `src/Platform/Microsoft.Testing.Platform` core (not just extensions) via a scoped explore-agent pass. Found and fixed: `TestCoverageResult.Scopes` (`Services/TestCoverageResult.cs`) built its per-invocation `List<(string,CoverageScope)>` (`order`) and `Dictionary<(string,CoverageScope),List<CoverageMetricResult>>` (`groups`) with default capacity despite `_measurementOrder.Count` being known upfront (worst-case upper bound on distinct groups). Fixed by pre-sizing both from `_measurementOrder.Count`. `TestCoverageResult` is internal — added `InternalsVisibleTo` grant for `MSTest.Performance.Benchmarks` in `Microsoft.Testing.Platform.csproj` (previously benchmarks project only had IVT from TestFramework/Adapter projects, not Platform itself).
- Added `TestCoverageResultBenchmarks` (small=5, large=1000 measurements, using `ConsumeAsync` to populate via the public `IDataConsumer` API) to `MSTest.Performance.Benchmarks`.
- Measured (in-process `-i`, ShortRun, net10.0 Release): Large (1000): 292.2 μs/506.93 KB -> 240.3 μs/345.31 KB (~18% faster, ~32% less alloc; baseline showed Gen2 collections that disappeared after the fix). Small (5): 1.143 μs/2.41 KB -> 1.117 μs/2.04 KB (noisy but directionally consistent).
- Verified: `Microsoft.Testing.Platform.UnitTests` (net9.0, Release) full suite — 2542/2563 passed, 21 pre-existing skips, all 10 `TestCoverageResultTests` pass. Both the platform project and the benchmarks project build cleanly. `dotnet format --verify-no-changes` on both projects shows only pre-existing unrelated warnings.
- **Environment note**: `--treenode-filter` is NOT recognized by `Microsoft.Testing.Platform.UnitTests.dll` in this repo (prints unrelated help text instead of erroring) — use VSTest-style `--filter "FullyQualifiedName~ClassName"` instead for this project (works correctly, confirmed). Other unit test projects in this repo may differ; check `--help` output for `--filter` vs `--treenode-filter` before assuming either works.
- Other explore-agent findings from the `src/Platform` scan, not yet implemented (queue for future run): `TestExecutionFilterComposer.cs:63-65` (`new HashSet<string>(uidFilter.TestNodeUids.Select(...), StringComparer.Ordinal)` — count known via `.Length` but wrapped in `.Select`, needs capacity-ctor + manual add loop rather than a trivial fix) and `ArtifactPostProcessingDispatcherTool.cs:187-190` (cold path, tiny collections, low value).
- PR created via safe-outputs from branch `perf-assist/testcoverageresult-scopes-presize` (title: "[perf-improver] Pre-size collections in TestCoverageResult.Scopes"). PR number not yet known — check next run.

## Round-robin task tracking (continued)
- 2026-09-24: Ran Task 2 (explore-agent scan of `src/Platform/Microsoft.Testing.Platform` core, found+fixed TestCoverageResult.Scopes), Task 3 (implemented + benchmarked + PR'd the fix), Task 4 (checked PRs #6/#11/#15/#18/#20 — no actionable CI/comments), Task 7 (recreated Monthly Activity issue for September since none existed — repo had 0 issues total). Task 1/5/6 not run this cycle — next run should prioritize Task 6 (measurement infra) and Task 5 (issue comments) since those have been deferred for many cycles now, and consider following up on `TestExecutionFilterComposer.cs`/`ArtifactPostProcessingDispatcherTool.cs` candidates noted above.

### 2026-09-25 (run 36141721928)
- Repo state check: found 20 open PRs total (many by `test-improver`/`efficiency-improver` sibling agents, not mine — labels `type/test-gap`/`area/performance` distinguish them). Confirmed my 6 open perf-improver PRs: #6, #11, #15, #18, #20, #23. Monthly Activity issue from 2026-09-24 no longer exists (0 issues found again via `list_issues`/`search_issues`) — recreated fresh.
- Task 4 (PR maintenance): checked PRs #6/#11/#15/#18/#20/#23 via `pull_request_read` (method=get/get_comments/get_status) — all still `state=open`, `draft=true`, `mergeable_state=unstable` (same pre-existing CI-gating pattern as every prior run), 0 comments each, CI status `pending`/empty `statuses` array (no per-check detail exposed via MCP tools). Nothing actionable, consistent with all prior runs since 2026-09-21.
- Task 2/3: Followed up on the 2026-09-24 queued candidate `TestExecutionFilterComposer.cs:63-65` (`new HashSet<string>(uidFilter.TestNodeUids.Select(uid => uid.Value), StringComparer.Ordinal)` — count known via `.Length` but wrapped in `.Select`). Implemented: hoisted `TestNodeUids` into a local, pre-sized the `HashSet<string>` from its `.Length` (guarded `#if NETCOREAPP3_1_OR_GREATER`, default ctor fallback on netstandard2.0), replaced the LINQ `.Select` with a manual `foreach` add loop.
- Added `TestExecutionFilterComposerBenchmarks` (small=5, large=1000 test-node uids, 2 overlapping provider filters) to `MSTest.Performance.Benchmarks`. Needed a new `InternalsVisibleTo` grant for `MSTest.Performance.Benchmarks` in `Microsoft.Testing.Platform.csproj` (same pattern as PR #23's `TestCoverageResult` grant — `TestExecutionFilterComposer` is internal).
- Measured (in-process `-i`, ShortRun, net10.0 Release): Large (1000 uids): 212.98 μs/274.28 KB -> 183.31 μs/124.80 KB (~14% faster, ~55% less alloc, Gen1 dropped 4.88->1.71 per 1000 ops). Small (5 uids): 1.446 μs/2.75 KB -> 1.241 μs/2.28 KB (noisy but directionally consistent).
- Verified: `Microsoft.Testing.Platform.UnitTests` (net9.0, Release) — all 20 `TestExecutionFilterComposerTests` pass; full suite 2542/2563 passed, 21 pre-existing skips (matches every prior run's baseline). Both platform and benchmarks projects build cleanly standalone. `dotnet format --verify-no-changes` on both projects shows only the same pre-existing CHARSET/IDE00xx warnings seen every run since 2026-09-22 — confirmed both modified/added files carry correct UTF-8 BOM and are not in the CHARSET error list.
- **Environment note (reconfirmed)**: standalone `dotnet build <project>.csproj -c Release -fnet10.0` on `Microsoft.Testing.Platform.csproj` fails with `MSB4036: GenerateFileFromTemplate task was not found` unless preceded by a full `./build.sh -c Release` in the same session — this applies even when just re-verifying a git-stashed baseline mid-run, not just on a fresh checkout. Needed two full `./build.sh -c Release` runs this session (once for baseline with the fix stashed, once after popping the stash) — each took ~1.5-6 min depending on restore cache state.
- PR created via safe-outputs from branch `perf-assist/testexecutionfiltercomposer-hashset-capacity` (title: "[perf-improver] Pre-size HashSet in TestExecutionFilterComposer uid intersection"). PR number not yet known — check next run.
- Backlog: the other 2026-09-24 queued candidate `ArtifactPostProcessingDispatcherTool.cs:187-190` remains unimplemented (cold path, tiny collections, explicitly flagged low-value) — not pursued, deprioritized indefinitely unless revisited with new evidence. No further "known-Count collection built with default capacity" instances found or queued this run — the systematic scan across TestFramework/Adapter/Platform (started 2026-09-22, continued 2026-09-23/24) is now considered **substantially complete**; future Task 2 runs should look for a *different* class of optimization opportunity rather than re-scanning for this same pattern.

## Round-robin task tracking (continued)
- 2026-09-25: Ran Task 2/3 (implemented + benchmarked + PR'd the TestExecutionFilterComposer fix, closing out the last queued candidate from 2026-09-24), Task 4 (checked PRs #6/#11/#15/#18/#20/#23 — no actionable CI/comments, all `unstable`/`pending`/0 comments), Task 7 (recreated Monthly Activity issue for September since none existed again). Task 1/5/6 not run this cycle — Task 1 commands still validated/current from prior runs (no re-validation needed); Task 5 skipped (repo has 0 non-Monthly-Activity issues, nothing to comment on); Task 6 (measurement infra) deferred again — now overdue for 4+ cycles, should be next run's priority. Also worth starting a fresh Task 2 angle next run (e.g. profile discovery/execution end-to-end, or scan for LINQ-in-hot-loop patterns beyond collection-capacity sizing) since the capacity-presizing vein is now exhausted.
